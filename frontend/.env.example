# Visible at build-time by Vite (they'll be embedded in the bundle)
VITE_GOOGLE_CLIENT_ID=YOUR_GOOGLE_OAUTH_CLIENT_ID.apps.googleusercontent.com
# IMPORTANT: from the browser this should point to your backend public URL
# In your setup the backend Nginx maps to ${NGINX_PORT} from docker-compose.
VITE_API_BASE_URL=http://localhost:8085
